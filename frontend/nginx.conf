# frontend/nginx.conf
server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # Redirecionada rotas desconhecidas para o index.html do React
        try_files $uri $uri/ /index.html;
    }
}